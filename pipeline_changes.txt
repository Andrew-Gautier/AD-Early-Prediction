05-05-26 Changes Planned for preprocessing pipeline. 
Explanation. the current structure of preprocessing pipeline exists because there was a directory of raw partially processed csv files that had been created. Now, the entire pipeline needs to be called with the source csv file as a parameter, and an destination directory specified as before. 

Look at datasets/Dataset_pooled/pooled_CN.csv (first few rows) as a sample of what the old csv output would be. 
First replicate the logic with ID's and progression vectors. 
df = pd.read_csv('investigator_ftldlbd_nacc72.csv') is the new file location. 

Existing columns with no change needed. 
    - ID = NACCID
    - age = NACCAGEB
    - SEX,EDUC,ALCOHOL,BMI,MMSE,GDS,CDR,TOBAC30,BILLS,TAXES,SHOPPING,GAMES,STOVE,MEALPREP,EVENTS,PAYATTN,REMDATES,TRAVEL,NACCFAM,CVHATT,CVAFIB,DIABETES,HYPERCHO,HYPERTEN,B12DEF,DEPD,ANX,NACCTBI,SMOKYRS,RACE,age,HISPANIC,hearing,vision
    - note hearing and vision are composites of other variables from the original dataframe, see in preprocessing how this is implemnted. 
NEW columns
    - months since baseline. Use NACCFDYS - Days from initial visit to each follow-up visit to create a list of months since baseline so I can calculate the true visit intervals. e.g. [0.0, 13.11, 25.33, 37.06, 52.0]
    - NACCLIVS
    - COMMUN. 

run_pipeline() changes. 
    - lead time cohort is no longer going to be sliced from 6-10. Lead time cohort will be from the same visit range as the additional set. It will just be a 5% group that will be taken as evenly as possible from each visit cohort (i.e. 2visit, 3visit ... nvisit. ), trying to balance between four classes: CN non progressor, CN progressor, MCI nonprogressor, MCI progressor. 
    - min_age parameter from the top of run_pipeline() can filter this directly to drop out any below this. 
    - it should output files the same way as before. 
        lead_time_CN.csv        pooled_CN.csv           reverters_CN.csv
        lead_time_MCI_AD.csv    pooled_MCI_AD.csv       reverters_MCI_AD.csv
    - Look to keep things robust and remove or condense any unecessary functions. 
    

05-14
Notes: Added APOE4 variable, NACCNE4S, categorical. 
Dropped MMSE and BMI requirements. 
Fixed low memory warning on pandas df copying. 
Future work: Add new imputation strategies. remove old mean/most frequent ones? 

05-20

Next step on the pipeline will be to support MICE/and or more sophisticated imputation methods. Taking context from missingness variable analysis in demographics.ipynb. To clarify further, no imputation should take place in model.py it should be done here.  

05-26

Removing RFECV and not implementing MCTS for now. One large overhaul is needed, replacing the grid searching with bayestian optimization Optuna library. Still can use a preliminary coarse search, then feature elimination, with a final exhaustive sweep. 

Some smaller changes needed
visualization - SHAP variable charts. 
switching objective function to PR-ROC instead of AUROC. 